{
  "name": "tetris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "node scripts/pre-build.js",
    "build": "esbuild src/index.js --outdir=build/static --entry-names=[dir]/[name]-[hash] --bundle --minify --sourcemap --loader:.png=file",
    "postbuild": "node scripts/post-build.js",
    "start": "esbuild src/index.js --outdir=public/static --bundle --servedir=public --serve=3001 --loader:.png=file"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chroma-js": "^2.1.2",
    "esbuild": "^0.14.10",
    "three": "^0.136.0"
  },
  "devDependencies": {
    "prettier": "2.5.1"
  }
}
